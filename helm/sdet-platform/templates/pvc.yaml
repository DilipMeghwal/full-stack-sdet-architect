apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: allure-results-pvc
spec:
  accessModes:
    - {{ .Values.pvc.accessMode }}
  resources:
    requests:
      storage: {{ .Values.pvc.size }}